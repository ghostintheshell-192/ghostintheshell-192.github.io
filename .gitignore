# ---- Session / personal (never tracked) ----
.personal/
.memory-bank/

# NOTE: .development/ is now tracked (design docs live with the code).
# Only specific files are committed for now; the rest stays untracked until reviewed.

# ---- Shell & system dotfiles ----
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules
.ripgreprc

# ---- IDE ----
.idea
.vscode
.vs

# ---- Claude Code local / runtime files ----
.mcp.json
.claude/agents
.claude/settings.local.json

# ---- Personal scripts ----
.scripts/

# ---- Docs (empty placeholder for now) ----
docs/
